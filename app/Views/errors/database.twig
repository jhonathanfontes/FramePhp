<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erro no Banco de Dados</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .error-container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            text-align: center;
        }
        .error-header {
            background-color: #dc3545;
            color: #fff;
            padding: 20px;
            border-radius: 5px 5px 0 0;
        }
        .error-body {
            background-color: #f8f9fa;
            padding: 20px;
            border: 1px solid #dee2e6;
            border-top: none;
            border-radius: 0 0 5px 5px;
        }
        .error-code {
            font-size: 72px;
            font-weight: bold;
            color: #dc3545;
            margin: 20px 0;
        }
        .error-solution {
            background-color: #e9ecef;
            padding: 15px;
            border-radius: 5px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-header">
            <h1>Erro no Banco de Dados</h1>
        </div>
        <div class="error-body">
            <div class="error-code">{{ errorCode }}</div>
            <h2>Oops! Algo deu errado com o banco de dados</h2>
            <p class="lead">{{ error.message }}</p>

            <div class="error-solution">
                <h3>Possíveis soluções:</h3>
                <ul class="list-unstyled">
                    <li>✓ Verifique se o banco de dados está acessível</li>
                    <li>✓ Execute as migrações do banco de dados</li>
                    <li>✓ Verifique as credenciais de conexão</li>
                    <li>✓ Certifique-se que todas as tabelas necessárias existem</li>
                </ul>
            </div>

            {% if debug %}
                <div class="alert alert-info mt-4">
                    <h3>Detalhes do Erro</h3>
                    <ul class="list-unstyled">
                        <li><strong>Mensagem Original:</strong> {{ error.message }}</li>
                        <li><strong>Arquivo:</strong> {{ error.file }}</li>
                        <li><strong>Linha:</strong> {{ error.line }}</li>
                        {% if error.sqlState is defined %}
                            <li><strong>SQL State:</strong> {{ error.sqlState }}</li>
                        {% endif %}
                    </ul>
                </div>
            {% endif %}

            <div class="mt-4">
                <a href="/" class="btn btn-primary">Voltar para Home</a>
                <a href="javascript:history.back()" class="btn btn-secondary">Voltar</a>
            </div>
        </div>
    </div>
</body>
</html>