{% extends 'errors/layouts/error.twig' %}

{% set code = 500 %}
{% set title = 'Erro de Conexão com o Banco de Dados' %}
{% set color = 'danger' %}

{% block error_icon %}
    <i class="fas fa-database"></i>
{% endblock %}

{% block error_help %}
    <div class="error-help">
        <h3><i class="fas fa-lightbulb"></i> Possíveis soluções:</h3>
        <ul>
            <li><i class="fas fa-check-circle"></i> Verifique se o servidor de banco de dados está em execução</li>
            <li><i class="fas fa-check-circle"></i> Confirme se as credenciais no arquivo .env estão corretas</li>
            <li><i class="fas fa-check-circle"></i> Verifique se o host configurado tem permissão para acessar o banco de dados</li>
            <li><i class="fas fa-check-circle"></i> Certifique-se de que o banco de dados especificado existe</li>
            <li><i class="fas fa-check-circle"></i> Verifique se o usuário tem permissões adequadas</li>
        </ul>
    </div>
{% endblock %}

<a href="{{ base_url() }}" class="home-link">Voltar para a página inicial</a>
        
        {% if app_debug() %}
        <div class="debug-info">
            <h3>Informações de Depuração:</h3>
            <p><strong>Arquivo:</strong> {{ error.file }}</p>
            <p><strong>Linha:</strong> {{ error.line }}</p>
            {% if error.trace is defined %}
            <h4>Stack Trace:</h4>
            <div>{{ error.trace }}</div>
            {% endif %}
        </div>
        {% endif %}
        
        <div class="error-details">
            {{ app_name() }} v{{ app_version() }}
        </div>
    </div>
</body>
</html>