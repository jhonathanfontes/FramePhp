{% extends "admin/layout.twig" %}

{% block title %}Dashboard - {{ empresa.nome_fantasia }}{% endblock %}
{% block page_title %}Dashboard{% endblock %}

{% block content %}
    <!-- Cards de Estatísticas -->
    <section class="stats-section" aria-labelledby="stats-title">
        <div class="grid grid-4">
            <div class="stat-card">
                <div class="stat-icon">💰</div>
                <div class="stat-content">
                    <h3 class="stat-title">Vendas Hoje</h3>
                    <p class="stat-value">{{ vendasHoje }}</p>
                    <span class="stat-change positive">+12% vs ontem</span>
                </div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">📊</div>
                <div class="stat-content">
                    <h3 class="stat-title">Vendas do Mês</h3>
                    <p class="stat-value">{{ vendasMes }}</p>
                    <span class="stat-change positive">+8% vs mês passado</span>
                </div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">💵</div>
                <div class="stat-content">
                    <h3 class="stat-title">Receita do Mês</h3>
                    <p class="stat-value">R$ {{ "%.2f"|format(receitaMes) }}</p>
                    <span class="stat-change positive">+15% vs mês passado</span>
                </div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">⚠️</div>
                <div class="stat-content">
                    <h3 class="stat-title">Baixo Estoque</h3>
                    <p class="stat-value">{{ produtosBaixoEstoque }}</p>
                    <span class="stat-change negative">Atenção necessária</span>
                </div>
            </div>
        </div>
    </section>

    <div class="dashboard-layout">
        <!-- Vendas Recentes -->
        <section class="dashboard-section" aria-labelledby="vendas-title">
            <div class="section-header">
                <h2 id="vendas-title" class="section-title">Vendas Recentes</h2>
                <a href="/admin/vendas" class="btn btn-primary">Ver Todas</a>
            </div>
            
            {% if vendasRecentes %}
                <div class="table-container">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Pedido</th>
                                <th>Cliente</th>
                                <th>Total</th>
                                <th>Status</th>
                                <th>Data</th>
                                <th>Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for venda in vendasRecentes %}
                                <tr>
                                    <td>
                                        <a href="/admin/vendas/{{ venda.id }}" class="table-link">
                                            #{{ venda.numero_pedido }}
                                        </a>
                                    </td>
                                    <td>
                                        {{ venda.pessoa ? venda.pessoa.nome : 'Cliente não identificado' }}
                                    </td>
                                    <td class="table-price">
                                        R$ {{ "%.2f"|format(venda.total) }}
                                    </td>
                                    <td>
                                        <span class="status-badge status-{{ venda.status }}">
                                            {{ venda.status|title }}
                                        </span>
                                    </td>
                                    <td>{{ venda.data_venda|date('d/m/Y H:i') }}</td>
                                    <td>
                                        <div class="table-actions">
                                            <a href="/admin/vendas/{{ venda.id }}" 
                                               class="btn btn-icon" 
                                               aria-label="Ver detalhes">
                                                👁️
                                            </a>
                                            <a href="/admin/vendas/{{ venda.id }}/edit" 
                                               class="btn btn-icon" 
                                               aria-label="Editar">
                                                ✏️
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% else %}
                <div class="empty-state">
                    <div class="empty-icon">📊</div>
                    <h3>Nenhuma venda encontrada</h3>
                    <p>Comece a registrar vendas para ver estatísticas aqui</p>
                </div>
            {% endif %}
        </section>

        <!-- Produtos Mais Vendidos -->
        <section class="dashboard-section" aria-labelledby="produtos-title">
            <div class="section-header">
                <h2 id="produtos-title" class="section-title">Produtos Mais Vendidos</h2>
                <a href="/admin/produtos" class="btn btn-primary">Ver Todos</a>
            </div>
            
            {% if produtosMaisVendidos %}
                <div class="table-container">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Produto</th>
                                <th>Código</th>
                                <th>Preço</th>
                                <th>Estoque</th>
                                <th>Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for produto in produtosMaisVendidos %}
                                <tr>
                                    <td>
                                        <a href="/admin/produtos/{{ produto.id }}" class="table-link">
                                            {{ produto.nome }}
                                        </a>
                                    </td>
                                    <td>{{ produto.codigo }}</td>
                                    <td class="table-price">
                                        R$ {{ "%.2f"|format(produto.preco) }}
                                    </td>
                                    <td>
                                        <span class="stock-badge {% if produto.estoque <= produto.estoque_minimo %}stock-low{% else %}stock-ok{% endif %}">
                                            {{ produto.estoque }}
                                        </span>
                                    </td>
                                    <td>
                                        <div class="table-actions">
                                            <a href="/admin/produtos/{{ produto.id }}" 
                                               class="btn btn-icon" 
                                               aria-label="Ver detalhes">
                                                👁️
                                            </a>
                                            <a href="/admin/produtos/{{ produto.id }}/edit" 
                                               class="btn btn-icon" 
                                               aria-label="Editar">
                                                ✏️
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% else %}
                <div class="empty-state">
                    <div class="empty-icon">📦</div>
                    <h3>Nenhum produto vendido</h3>
                    <p>Adicione produtos e registre vendas para ver estatísticas</p>
                </div>
            {% endif %}
        </section>
    </div>

    <!-- Gráficos e Análises -->
    <section class="charts-section" aria-labelledby="charts-title">
        <h2 id="charts-title" class="section-title">Análises</h2>
        
        <div class="grid grid-2">
            <!-- Gráfico de Vendas -->
            <div class="chart-card card">
                <div class="chart-header">
                    <h3>Vendas dos Últimos 7 Dias</h3>
                    <div class="chart-legend">
                        <span class="legend-item">
                            <span class="legend-color"></span>
                            Vendas
                        </span>
                    </div>
                </div>
                <div class="chart-container">
                    <div class="chart-bars">
                        <div class="chart-bar" style="height: 60px;" data-value="15">
                            <span class="bar-label">Seg</span>
                            <span class="bar-value">15</span>
                        </div>
                        <div class="chart-bar" style="height: 80px;" data-value="20">
                            <span class="bar-label">Ter</span>
                            <span class="bar-value">20</span>
                        </div>
                        <div class="chart-bar" style="height: 120px;" data-value="30">
                            <span class="bar-label">Qua</span>
                            <span class="bar-value">30</span>
                        </div>
                        <div class="chart-bar" style="height: 90px;" data-value="22">
                            <span class="bar-label">Qui</span>
                            <span class="bar-value">22</span>
                        </div>
                        <div class="chart-bar" style="height: 150px;" data-value="38">
                            <span class="bar-label">Sex</span>
                            <span class="bar-value">38</span>
                        </div>
                        <div class="chart-bar" style="height: 100px;" data-value="25">
                            <span class="bar-label">Sáb</span>
                            <span class="bar-value">25</span>
                        </div>
                        <div class="chart-bar" style="height: 130px;" data-value="32">
                            <span class="bar-label">Dom</span>
                            <span class="bar-value">32</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Status do Sistema -->
            <div class="status-card card">
                <h3>Status do Sistema</h3>
                <div class="status-list">
                    <div class="status-item">
                        <div class="status-info">
                            <span class="status-label">Produtos Ativos</span>
                            <span class="status-value">{{ empresa.produtos().where('ativo', true).count() }}</span>
                        </div>
                        <div class="status-indicator success"></div>
                    </div>
                    
                    <div class="status-item">
                        <div class="status-info">
                            <span class="status-label">Clientes Cadastrados</span>
                            <span class="status-value">{{ empresa.pessoas().where('ativo', true).count() }}</span>
                        </div>
                        <div class="status-indicator success"></div>
                    </div>
                    
                    <div class="status-item">
                        <div class="status-info">
                            <span class="status-label">Pedidos Pendentes</span>
                            <span class="status-value">{{ empresa.vendas().where('status', 'pendente').count() }}</span>
                        </div>
                        <div class="status-indicator warning"></div>
                    </div>
                    
                    <div class="status-item">
                        <div class="status-info">
                            <span class="status-label">Baixo Estoque</span>
                            <span class="status-value">{{ produtosBaixoEstoque }}</span>
                        </div>
                        <div class="status-indicator {% if produtosBaixoEstoque > 0 %}danger{% else %}success{% endif %}"></div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Ações Rápidas -->
    <section class="actions-section" aria-labelledby="actions-title">
        <h2 id="actions-title" class="section-title">Ações Rápidas</h2>
        <div class="actions-grid">
            <a href="/admin/produtos/create" class="action-card">
                <div class="action-icon">➕</div>
                <h3>Novo Produto</h3>
                <p>Cadastrar novo produto no sistema</p>
            </a>
            
            <a href="/admin/vendas/create" class="action-card">
                <div class="action-icon">💰</div>
                <h3>Nova Venda</h3>
                <p>Registrar nova venda manualmente</p>
            </a>
            
            <a href="/admin/pessoas/create" class="action-card">
                <div class="action-icon">👥</div>
                <h3>Novo Cliente</h3>
                <p>Cadastrar novo cliente</p>
            </a>
            
            <a href="/admin/estoque" class="action-card">
                <div class="action-icon">📋</div>
                <h3>Controle de Estoque</h3>
                <p>Gerenciar estoque dos produtos</p>
            </a>
            
            <a href="/admin/relatorios" class="action-card">
                <div class="action-icon">📈</div>
                <h3>Relatórios</h3>
                <p>Gerar relatórios detalhados</p>
            </a>
            
            <a href="/admin/configuracoes" class="action-card">
                <div class="action-icon">⚙️</div>
                <h3>Configurações</h3>
                <p>Configurar sistema</p>
            </a>
        </div>
    </section>

    <style>
        /* ===== STATS SECTION ===== */
        .stats-section {
            margin-bottom: var(--spacing-xl);
        }
        
        .stat-card {
            background: var(--bg-white);
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow);
            padding: var(--spacing-lg);
            display: flex;
            align-items: center;
            gap: var(--spacing-md);
            transition: var(--transition);
        }
        
        .stat-card:hover {
            box-shadow: var(--shadow-hover);
            transform: translateY(-2px);
        }
        
        .stat-icon {
            font-size: 2.5rem;
            opacity: 0.8;
        }
        
        .stat-content {
            flex: 1;
        }
        
        .stat-title {
            font-size: 0.9rem;
            color: var(--text-light);
            margin-bottom: var(--spacing-xs);
            font-weight: 500;
        }
        
        .stat-value {
            font-size: 2rem;
            font-weight: bold;
            color: var(--text-color);
            margin-bottom: var(--spacing-xs);
        }
        
        .stat-change {
            font-size: 0.8rem;
            font-weight: 500;
        }
        
        .stat-change.positive {
            color: #28a745;
        }
        
        .stat-change.negative {
            color: #dc3545;
        }
        
        /* ===== DASHBOARD LAYOUT ===== */
        .dashboard-layout {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: var(--spacing-lg);
            margin-bottom: var(--spacing-xl);
        }
        
        /* ===== DASHBOARD SECTIONS ===== */
        .dashboard-section {
            background: var(--bg-white);
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow);
            overflow: hidden;
        }
        
        .section-header {
            padding: var(--spacing-lg);
            border-bottom: 1px solid var(--border-color);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .section-title {
            font-size: 1.3rem;
            color: var(--text-color);
            margin: 0;
        }
        
        /* ===== DATA TABLE ===== */
        .table-container {
            overflow-x: auto;
        }
        
        .data-table {
            width: 100%;
            border-collapse: collapse;
        }
        
        .data-table th,
        .data-table td {
            padding: var(--spacing-sm) var(--spacing-md);
            text-align: left;
            border-bottom: 1px solid var(--border-color);
        }
        
        .data-table th {
            background-color: var(--bg-light);
            font-weight: 600;
            color: var(--text-color);
            font-size: 0.9rem;
        }
        
        .data-table tr:hover {
            background-color: var(--bg-light);
        }
        
        .table-link {
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 500;
        }
        
        .table-link:hover {
            text-decoration: underline;
        }
        
        .table-price {
            font-weight: bold;
            color: var(--primary-color);
        }
        
        .table-actions {
            display: flex;
            gap: var(--spacing-xs);
        }
        
        /* ===== STATUS BADGES ===== */
        .status-badge {
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 500;
        }
        
        .status-pendente { background: #fff3cd; color: #856404; }
        .status-aprovado { background: #d1ecf1; color: #0c5460; }
        .status-em_preparo { background: #d4edda; color: #155724; }
        .status-enviado { background: #cce5ff; color: #004085; }
        .status-entregue { background: #d4edda; color: #155724; }
        .status-cancelado { background: #f8d7da; color: #721c24; }
        
        .stock-badge {
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 500;
        }
        
        .stock-ok { background: #d4edda; color: #155724; }
        .stock-low { background: #f8d7da; color: #721c24; }
        
        /* ===== EMPTY STATE ===== */
        .empty-state {
            text-align: center;
            padding: var(--spacing-xl);
            color: var(--text-light);
        }
        
        .empty-icon {
            font-size: 3rem;
            margin-bottom: var(--spacing-md);
            opacity: 0.5;
        }
        
        .empty-state h3 {
            color: var(--text-color);
            margin-bottom: var(--spacing-sm);
        }
        
        /* ===== CHARTS SECTION ===== */
        .charts-section {
            margin-bottom: var(--spacing-xl);
        }
        
        .chart-card {
            position: relative;
        }
        
        .chart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: var(--spacing-lg);
        }
        
        .chart-header h3 {
            color: var(--text-color);
            margin: 0;
        }
        
        .chart-legend {
            display: flex;
            gap: var(--spacing-sm);
        }
        
        .legend-item {
            display: flex;
            align-items: center;
            gap: var(--spacing-xs);
            font-size: 0.9rem;
            color: var(--text-light);
        }
        
        .legend-color {
            width: 12px;
            height: 12px;
            background: var(--primary-color);
            border-radius: 2px;
        }
        
        .chart-container {
            height: 300px;
            display: flex;
            align-items: end;
            justify-content: space-around;
            padding: var(--spacing-lg);
        }
        
        .chart-bars {
            display: flex;
            align-items: end;
            gap: var(--spacing-sm);
            height: 100%;
            width: 100%;
        }
        
        .chart-bar {
            background: var(--primary-color);
            border-radius: var(--border-radius) var(--border-radius) 0 0;
            position: relative;
            min-width: 40px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: end;
            padding-bottom: var(--spacing-sm);
        }
        
        .bar-label {
            font-size: 0.8rem;
            color: var(--text-light);
            margin-bottom: var(--spacing-xs);
        }
        
        .bar-value {
            font-size: 0.9rem;
            font-weight: bold;
            color: var(--text-color);
        }
        
        /* ===== STATUS CARD ===== */
        .status-card h3 {
            color: var(--text-color);
            margin-bottom: var(--spacing-lg);
        }
        
        .status-list {
            display: flex;
            flex-direction: column;
            gap: var(--spacing-md);
        }
        
        .status-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: var(--spacing-sm);
            border-radius: var(--border-radius);
            background: var(--bg-light);
        }
        
        .status-info {
            display: flex;
            flex-direction: column;
            gap: 0.2rem;
        }
        
        .status-label {
            font-size: 0.9rem;
            color: var(--text-light);
        }
        
        .status-value {
            font-weight: bold;
            color: var(--text-color);
        }
        
        .status-indicator {
            width: 12px;
            height: 12px;
            border-radius: 50%;
        }
        
        .status-indicator.success { background: #28a745; }
        .status-indicator.warning { background: #ffc107; }
        .status-indicator.danger { background: #dc3545; }
        
        /* ===== ACTIONS SECTION ===== */
        .actions-section {
            margin-bottom: var(--spacing-xl);
        }
        
        .actions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: var(--spacing-md);
        }
        
        .action-card {
            background: var(--bg-white);
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow);
            padding: var(--spacing-lg);
            text-align: center;
            text-decoration: none;
            color: var(--text-color);
            transition: var(--transition);
        }
        
        .action-card:hover {
            box-shadow: var(--shadow-hover);
            transform: translateY(-2px);
        }
        
        .action-icon {
            font-size: 2.5rem;
            margin-bottom: var(--spacing-sm);
        }
        
        .action-card h3 {
            margin-bottom: var(--spacing-xs);
            color: var(--text-color);
        }
        
        .action-card p {
            color: var(--text-light);
            font-size: 0.9rem;
        }
        
        /* ===== RESPONSIVIDADE ===== */
        @media (max-width: 1024px) {
            .dashboard-layout {
                grid-template-columns: 1fr;
            }
        }
        
        @media (max-width: 768px) {
            .grid-4 {
                grid-template-columns: repeat(2, 1fr);
            }
            
            .actions-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            
            .chart-container {
                height: 200px;
            }
            
            .chart-bar {
                min-width: 30px;
            }
            
            .bar-label {
                font-size: 0.7rem;
            }
            
            .bar-value {
                font-size: 0.8rem;
            }
        }
        
        @media (max-width: 480px) {
            .grid-4 {
                grid-template-columns: 1fr;
            }
            
            .actions-grid {
                grid-template-columns: 1fr;
            }
            
            .stat-card {
                flex-direction: column;
                text-align: center;
            }
            
            .table-container {
                font-size: 0.9rem;
            }
            
            .data-table th,
            .data-table td {
                padding: var(--spacing-xs);
            }
        }
    </style>

    <script>
        // Animar barras do gráfico
        function animarGrafico() {
            const barras = document.querySelectorAll('.chart-bar');
            barras.forEach((barra, index) => {
                setTimeout(() => {
                    barra.style.opacity = '0';
                    barra.style.transform = 'scaleY(0)';
                    
                    setTimeout(() => {
                        barra.style.transition = 'all 0.5s ease';
                        barra.style.opacity = '1';
                        barra.style.transform = 'scaleY(1)';
                    }, 100);
                }, index * 100);
            });
        }
        
        // Atualizar dados em tempo real
        function atualizarDashboard() {
            // Aqui você pode implementar atualizações em tempo real
            console.log('Dashboard atualizado');
        }
        
        // Inicializar
        document.addEventListener('DOMContentLoaded', function() {
            animarGrafico();
            
            // Atualizar a cada 30 segundos
            setInterval(atualizarDashboard, 30000);
        });
        
        // Tooltip para barras do gráfico
        document.querySelectorAll('.chart-bar').forEach(barra => {
            const valor = barra.getAttribute('data-value');
            barra.addEventListener('mouseenter', function() {
                this.style.transform = 'scaleY(1.1)';
            });
            
            barra.addEventListener('mouseleave', function() {
                this.style.transform = 'scaleY(1)';
            });
        });
    </script>
{% endblock %} 